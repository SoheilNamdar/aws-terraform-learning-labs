LocalStack + Terraform learning lab

This folder is a small sandbox to learn Terraform locally using LocalStack.

Next steps:
1. Add `docker-compose.yml` to run LocalStack.
2. Configure `main.tf` provider to point to LocalStack.
3. Add example resources (e.g., `s3.tf`).
4. Run LocalStack and use `terraform init` / `apply` to test.

Status: Step 1 (folder) created.